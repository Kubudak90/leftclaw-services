# Private key for the test wallet (must have ~$2 USDC on Base — price is ~$1, fetched live from contract)
# NEVER commit the real .env file — only this .example template
TEST_PRIVATE_KEY=0xYOUR_PRIVATE_KEY_HERE
